/*jslint browser:true, vars: true, nomen: true */
"use strict";

if (!window.PM) {
    window.PM = {};
}

if (!window.PM.config) {
    window.PM.config = {};
}

(function (config) {
    config.is_master = true;
    config.master_name = "Party Owner";
}(window.PM.config));
